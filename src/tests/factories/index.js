import { buildAlbum, createAlbum } from './album.factory';
import { buildConcert, createConcert } from './concert.factory';
import { buildMusician, createMusician } from './musician.factory';
import { buildSong, createSong } from './song.factory';

export {
  buildMusician,
  createMusician,
  buildAlbum,
  createAlbum,
  buildSong,
  createSong,
  buildConcert,
  createConcert,
};
